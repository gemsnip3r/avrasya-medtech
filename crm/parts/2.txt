d="customers">+ Müşteri</button>`)+`<div class="panel glass"><div class="list">${arr.map(rowCustomer).join('')||'<div class="empty">Kayıt bulunamadı</div>'}</div></div>`}
function pipeline(){return head('Pipeline','Fırsatları satış aşamasına göre yönetin',`<button class="btn" data-add="customers">+ Lead</button>`)+`<div class="kanban">${stages.map(st=>`<div class="col"><div class="colHead"><b>${st}</b><span>${data.customers.filter(c=>(c.stage||'Yeni Lead')===st).length}</span></div>${data.customers.filter(c=>(c.stage||'Yeni Lead')===st).map(c=>`<div class="card"><b>${esc(c.name)}</b><small>${esc(c.company||'')} · ${money(c.value||0)}</small><select class="input stage" data-id="${c.id}" style="margin-top:9px;padding:8px">${stages.map(s=>`<option ${s===(c.stage||'Yeni Lead')?'selected':''}>${s}</option>`).join('')}</select></div>`).join('')}</div>`).join('')}</div>`}
function simplePage(kind,title,sub,cols){let rows=data[kind];return head(title,sub,`<button class="btn" data-add="${kind}">+ Yeni</button>`)+`<div class="panel glass tableWrap"><table><thead><tr>${cols.map(c=>`<th>${c[1]}</th>`).join('')}<th></th></tr></thead><tbody>${rows.map(r=>`<tr>${cols.map(c=>`<td>${c[2]?c[2](r[c[0]],r):esc(r[c[0]]||'—')}</td>`).join('')}<td><button class="btn secondary sm" data-edit="${kind}" data-id="${r.id}">Düzenle</button> <button class="btn danger sm" data-del="${kind}" data-id="${r.id}">Sil</button></td></tr>`).join('')||`<tr><td colspan="${cols.length+1}" class="empty">Kayıt yok</td></tr>`}</tbody></table></div>`}
const tasks=()=>simplePage('tasks','Görevler','Ekip aksiyonları',[['title','Görev'],['customer','Müşteri'],['date','Tarih'],['priority','Öncelik']]);const quotes=()=>simplePage('quotes','Teklifler','Aktif ticari teklifler',[['customer','Müşteri'],['product','Ürün'],['amount','Tutar',money],['status','Durum']]);const sales=()=>simplePage('sales','Satışlar','Gerçekleşen satış kayıtları',[['customer','Müşteri'],['product','Ürün'],['amount','Tutar',money],['date','Tarih']]);const events=()=>simplePage('events','Takvim','Görüşme, demo ve kurulum planı',[['title','Etkinlik'],['customer','Müşteri'],['date','Tarih'],['time','Saat']]);const dealers=()=>simplePage('dealers','Bayiler','Bölgesel bayi ağı',[['name','Bayi'],['city','Şehir'],['contact','Yetkili'],['status','Durum']]);
function userPage(){return head('Kullanıcılar','Rol ve erişim yönetimi',`<button class="btn" data-add="users">+ Kullanıcı</button>`)+`<div class="panel glass tableWrap"><table><thead><tr><th>Ad</th><th>E-posta</th><th>Rol</th><th>Bölge</th><th></th></tr></thead><tbody>${users.map(u=>`<tr><td>${esc(u.name)}</td><td>${esc(u.email)}</td><td><span class="badge">${esc(u.role)}</span></td><td>${esc(u.region)}</td><td><button class="btn secondary sm" data-edit="users" data-id="${u.id}">Düzenle</button></td></tr>`).join('')}</tbody></table></div>`}
const forms={customers:[['name','Ad Soyad','text'],['company','Klinik','text'],['phone','Telefon','tel'],['city','Şehir','text'],['stage','Aşama','select',stages],['value','Potansiyel Tutar','number']],tasks:[['title','Görev','text'],['customer','Müşteri','text'],['date','Tarih','date'],['priority','Öncelik','select',['Normal','Önemli','Acil']]],quotes:[['customer','Müşteri','text'],['product','Ürün','text'],['amount','Tutar','number'],['status','Durum','select',['Hazırlandı','Gönderildi','Kazanıldı','Kaybedildi']]],sales:[['customer','Müşteri','text'],['product','Ürün','text'],['amount','Tutar','number'],['date','Tarih','date']],events:[['title','Etkinlik','text'],['customer','Müşteri','text'],['date','Tarih','date'],['time','Saat','time']],dealers:[['name','Bayi','text'],['city','Şehir','text'],['contact','Yetkili','text'],['status','Durum','select',['Aktif','Pasif']]],users:[['name','Ad Soyad','text'],['email','E-posta','email'],['role','Rol','select',['Admin','Satış Müdürü','Satış Temsilcisi','Bayi']],['region','Bölge','text'],['password','Şifre','password']]};
function openForm(kind,id){let obj=kind==='users'?users.find(x=>x.id==id):data[kind].find(x=>x.id==id);obj=obj||{};$('#modalTitle').textContent=(id?'Düzenle: ':'Yeni ')+(labels[kind]||kind);$('#modalForm').innerHTML=forms[kind].map(f=>`<div class="field ${f[0]==='password'?'full':''}"><label>${f[1]}</label>${f[2]==='select'?`<select class="input" name="${f[0]}">${f[3].map(o=>`<option ${o===obj[f[0]]?'selected':''}>${o}</option>`).join('')}</select>`:`<input class="input" name="${f[0]}" type="${f[2]}" value="${esc(f[0]==='password'?'':obj[f[0]]||'')}">`}</div>`).join('')+`<div class="full"><button class="btn" style="width:100%">Kaydet</button></div>`;$('#modalForm').onsubmit=async e=>{e.preventDefault();let v=Object.fromEntries(new FormData(e.target));try{if(kind==='users'){if(id)await api('/api/crm/users',{method:'PATCH',body:JSON.stringify({id,...v})});else await api('/api/crm/users',{method:'POST',body:JSON.stringify(v)});users=(await api('/api/crm/users')).users||[]}else{let a=data[kind],i=a.findIndex(x=>x.id==id);let n={...(i>=0?a[i]:{}),...v,id:id||crypto.randomUUID()};if(i>=0)a[i]=n;else a.push(n);await save(kind)}closeModal();render();toast('Kaydedildi')}catch(x){toast(x.message)}};$('#modalBack').classList.remove('hide')}
function closeModal(){$('#modalBack').classList.add('hide')}async function del(kind,id){if(!confirm('Kayıt silinsin mi?'))return;data[kind]=data[kind].filter(x=>x.id!=id);await save(kind);render();toast('Silindi')}
function bind(){document.querySelectorAll('[data-add]').forEach(b=>b.onclick=()=>openForm(b.dataset.add));document.querySelectorAll('[data-edit]').forEach(b=>b.onclick=()=>openForm(b.dataset.edit,b.dataset.id));document.querySelectorAll('[data-del]').forEach(b=>b.onclick=()=>del(b.dataset.del,b.dataset.id));document.querySelectorAll('.stage').forEach(s=>s.onchange=async()=>{let c=data.customers.find(x=>x.id==s.dataset.id);if(c){c.stage=s.value;await save('customers');render();toast('Aşama güncellendi')}})}
$('#loginForm').onsubmit=async e=>{e.preventDefault();$('#loginErr').textContent='';try{await login($('#le').value,$('#lp').value)}catch(x){$('#loginErr').textContent=x.message}};$('#logout').onclick=async()=>{await api('/api/crm/auth',{method:'DELETE'}).catch(()=>{});location.reload()};$('#refresh').onclick=async()=>{await load();render();toast('Senkronize edildi')};$('#search').oninput=()=>{if(page==='customers')render()};restore();
</script></body></html>
