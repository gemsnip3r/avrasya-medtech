d><body>
<div id="login" class="login"><form id="loginForm" class="loginCard glass"><div class="brand"><div class="brandMark"></div><span>AVRASYA MEDTECH</span></div><h1>Commercial OS</h1><p class="sub">CRM sistemine güvenli giriş yapın.</p><div class="field"><label>E-posta</label><input id="le" class="input" type="email" autocomplete="username" required></div><div class="field"><label>Şifre</label><input id="lp" class="input" type="password" autocomplete="current-password" required></div><button class="btn" style="width:100%;margin-top:10px">Giriş Yap</button><p id="loginErr" class="red"></p></form></div>
<div id="app" class="shell hide"><aside class="side"><div class="brand"><div class="brandMark"></div><span>AVRASYA CRM</span></div><div id="nav" class="nav"></div><div class="sideFoot glass"><small class="mut">Neon PostgreSQL</small><b style="display:block">Server Sync Active</b></div></aside><main class="main"><header class="top"><div class="topSearch"><input id="search" class="input" placeholder="Müşteri, klinik veya şehir ara..."></div><button id="refresh" class="btn secondary sm">↻ Yenile</button><div class="profile"><div><b id="uName"></b><small id="uRole" class="mut" style="display:block"></small></div><div id="avatar" class="avatar"></div><button id="logout" class="btn secondary sm">Çıkış</button></div></header><section id="view" class="content"></section></main><nav id="bottom" class="bottom glass"></nav></div>
<div id="modalBack" class="modalBack hide"><div class="modal glass"><div class="modalHead"><h3 id="modalTitle"></h3><button class="btn secondary sm" onclick="closeModal()">✕</button></div><form id="modalForm" class="modalGrid"></form></div></div><div id="toast" class="toast hide"></div>
<script>
const icons={dashboard:'◫',customers:'◎',pipeline:'◇',tasks:'✓',quotes:'▤',sales:'↗',events:'□',dealers:'⌂',users:'♙'};const labels={dashboard:'Dashboard',customers:'Müşteriler',pipeline:'Pipeline',tasks:'Görevler',quotes:'Teklifler',sales:'Satışlar',events:'Takvim',dealers:'Bayiler',users:'Kullanıcılar'};const stages=['Yeni Lead','Görüşülüyor','Demo','Teklif','Kazanıldı'];let user=null,page='dashboard',data={customers:[],tasks:[],quotes:[],sales:[],events:[],dealers:[],notifications:[],activities:[]},users=[];
const $=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m])),money=n=>new Intl.NumberFormat('tr-TR',{style:'currency',currency:'TRY',maximumFractionDigits:0}).format(+n||0),today=()=>new Date().toISOString().slice(0,10);
async function api(url,opt={}){let r=await fetch(url,{credentials:'same-origin',headers:{'Content-Type':'application/json',...(opt.headers||{})},...opt});let j=await r.json().catch(()=>({}));if(!r.ok)throw Error(j.error||'İşlem başarısız');return j}function toast(t){let e=$('#toast');e.textContent=t;e.classList.remove('hide');setTimeout(()=>e.classList.add('hide'),2200)}
async function login(email,password){let j=await api('/api/crm/auth',{method:'POST',body:JSON.stringify({email,password})});user=j.user;await load();showApp()}async function restore(){try{user=(await api('/api/crm/auth')).user;await load();showApp()}catch{}}async function load(){let j=await api('/api/crm/data');Object.assign(data,j.data||{});for(let k of Object.keys(data))if(!Array.isArray(data[k]))data[k]=[];if(user.role==='Admin')try{users=(await api('/api/crm/users')).users||[]}catch{users=[]}}
async function save(kind){await api('/api/crm/data',{method:'PUT',body:JSON.stringify({kind,records:data[kind]})})}function showApp(){$('#login').classList.add('hide');$('#app').classList.remove('hide');$('#uName').textContent=user.name;$('#uRole').textContent=user.role;$('#avatar').textContent=(user.name||'A').split(' ').map(x=>x[0]).slice(0,2).join('');buildNav();render()}
function allowed(){let a=['dashboard','customers','pipeline','tasks','quotes','sales','events'];if(['Admin','Satış Müdürü'].includes(user.role))a.push('dealers');if(user.role==='Admin')a.push('users');return a}function buildNav(){let a=allowed();$('#nav').innerHTML=a.map(x=>`<button data-p="${x}"><i>${icons[x]}</i><span>${labels[x]}</span></button>`).join('');$('#bottom').innerHTML=a.slice(0,5).map(x=>`<button data-p="${x}">${icons[x]}<br>${labels[x]}</button>`).join('');document.querySelectorAll('[data-p]').forEach(b=>b.onclick=()=>{page=b.dataset.p;render()})}
function head(title,sub,action=''){return `<div class="pageHead"><div><h2>${title}</h2><div class="mut">${sub}</div></div>${action}</div>`}function activeNav(){document.querySelectorAll('[data-p]').forEach(b=>b.classList.toggle('active',b.dataset.p===page))}
function render(){activeNav();let fn={dashboard:dash,customers:customers,pipeline:pipeline,tasks:tasks,quotes:quotes,sales:sales,events:events,dealers:dealers,users:userPage}[page]||dash;$('#view').innerHTML=fn();bind()}
function dash(){let c=data.customers.length,q=data.quotes.filter(x=>x.status!=='Kaybedildi').reduce((s,x)=>s+(+x.amount||0),0),s=data.sales.reduce((a,x)=>a+(+x.amount||0),0),due=data.tasks.filter(x=>!x.done&&(!x.date||x.date<=today())).length;return head('Dashboard','Satış operasyonunun canlı özeti',`<button class="btn" data-add="customers">+ Yeni Lead</button>`)+`<div class="grid4"><div class="kpi glass"><small>Toplam Müşteri</small><b>${c}</b><span class="green">Canlı CRM</span></div><div class="kpi glass"><small>Aktif Teklif</small><b>${money(q)}</b><span class="mut">${data.quotes.length} teklif</span></div><div class="kpi glass"><small>Toplam Satış</small><b>${money(s)}</b><span class="green">Gerçekleşen</span></div><div class="kpi glass"><small>Bekleyen Görev</small><b>${due}</b><span class="amber">Aksiyon gerekli</span></div></div><div class="layout"><div class="panel glass"><h3>Son Müşteriler</h3><div class="list">${data.customers.slice(-6).reverse().map(c=>rowCustomer(c)).join('')||'<div class="empty">Henüz müşteri yok</div>'}</div></div><div class="panel glass"><h3>Yaklaşan Görevler</h3><div class="list">${data.tasks.filter(x=>!x.done).slice(0,6).map(t=>`<div class="row"><div class="grow"><b>${esc(t.title)}</b><small>${esc(t.customer||'')} · ${esc(t.date||'Tarih yok')}</small></div><span class="badge">${esc(t.priority||'Normal')}</span></div>`).join('')||'<div class="empty">Görev yok</div>'}</div></div></div>`}
function rowCustomer(c){return `<div class="row"><div class="avatar">${esc((c.name||'M')[0])}</div><div class="grow"><b>${esc(c.name)}</b><small>${esc(c.company||'')} · ${esc(c.city||'')}</small></div><span class="badge">${esc(c.stage||'Yeni Lead')}</span><button class="btn secondary sm" data-edit="customers" data-id="${c.id}">Düzenle</button></div>`}
function customers(){let q=($('#search').value||'').toLowerCase(),arr=data.customers.filter(x=>[x.name,x.company,x.city,x.phone].join(' ').toLowerCase().includes(q));return head('Müşteriler','Klinik ve doktor portföyü',`<button class="btn" data-ad